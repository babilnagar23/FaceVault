# FaceVault Mobile

Flutter employee app foundation with offline-first attendance, biometric abstraction layers, mock repositories, and complete route coverage.

## Commands

```powershell
flutter pub get
flutter analyze
flutter test
flutter run
```

