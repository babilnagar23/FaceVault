# ─────────────────────────────────────────────────────────
# FaceVault API — Python Dependencies
# ─────────────────────────────────────────────────────────

# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Pydantic / Settings
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0

# Database — SQLAlchemy async
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2

# PostGIS / Geo
geoalchemy2==0.15.2
shapely==2.0.5

# Auth / Security
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
cryptography==42.0.8

# Redis / Caching
redis[hiredis]==5.0.7

# Celery (task queue)
celery[redis]==5.4.0
celery-beat-scheduler==2.0.0

# HTTP client
httpx==0.27.0

# Object Storage (S3/MinIO)
boto3==1.34.144
botocore==1.34.144

# Monitoring / Logging
structlog==24.2.0
sentry-sdk[fastapi]==2.10.0

# Utilities
python-dateutil==2.9.0
pytz==2024.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
factory-boy==3.3.0
faker==26.0.0

# Dev tooling
ruff==0.5.2
