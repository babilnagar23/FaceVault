# FaceVault Admin Website

Next.js admin dashboard foundation with protected routes, mock APIs, reusable enterprise UI components, reports, verification queue, help desk, location management, announcements, and settings.

## Commands

```powershell
npm install
npm run dev
npm run lint
npm run typecheck
npm run build
```

